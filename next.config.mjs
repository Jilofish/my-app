/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'images.pexels.com', 'www.wordstream.com', 'i.ytimg.com', 'cdn-8.motorsport.com' ,'cdn-9.motorsport.com']
    }
};

export default nextConfig;
